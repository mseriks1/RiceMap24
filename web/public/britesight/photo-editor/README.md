BriteSight Photo Editor v0.16.57

Adds an Adjust Presets menu below Adjust, with quick looks that apply real adjustment values without using the layer-filter slot. Also keeps the larger filter library, export controls, and multi-mask workflow.


Added: expanded adjust presets with stronger visible effect and fixed apply behavior.


Update in v0.16.57: refined photo-app branding by simplifying the top bar, removing visible version text, and shifting the stage toward the green family theme without changing functionality.
